<%= simple_form_for(@typeofuser) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :user_type %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
